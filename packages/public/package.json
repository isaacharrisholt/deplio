{
  "scripts": {
    "download": "./download.sh",
    "gen:api:typescript": "openapi-generator-cli generate --generator-key typescript",
    "gen:api": "pnpm run download && pnpm openapi-generator-cli generate"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.10.0"
  }
}
