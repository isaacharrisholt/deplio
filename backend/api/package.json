{
  "name": "api",
  "scripts": {
    "codegen": "poetry run cadwyn codegen deplio.models.versions:version_bundle",
    "dev": "doppler run -- poetry run uvicorn deplio.app:app --reload",
    "format": "poetry run ruff check --fix .; poetry run ruff format",
    "lint": "poetry run ruff check ."
  }
}
